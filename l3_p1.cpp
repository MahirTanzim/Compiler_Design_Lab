#include<bits/stdc++.h>

using namespace std;
int main() {
    string line;
    int count = 0;
    while (getline(cin, line)) {
        count++;
    }
    cout << "Total number of lines: " << count << endl;
    return 0;
}
